# CV Build Notes

Source files:

- `cv.typ`: Typst entrypoint
- `cv.yml`: structured CV data
- `publications.bib`: publication metadata

Build command:

```bash
typst compile cv/cv.typ cv/cv.pdf
```

Output:

- `cv/cv.pdf`
